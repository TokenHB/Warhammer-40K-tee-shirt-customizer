using UnityEngine;
using System.Collections;

public class Globals_set_up : MonoBehaviour {

	public string fraction_selected = "SM";
	public string Selected_view = "front";
	public bool faction_changed = false;

	// Use this for initialization
	void Start () {
		
		//return fraction_selected;
	}
	
	// Update is called once per frame
	void Update () {



	//	if (faction_changed) {
	//		Debug.Log (fraction_selected);
	//		Debug.Log (Selected_view);
			//faction_changed = false;
	//	}
	}

	//return;
}
